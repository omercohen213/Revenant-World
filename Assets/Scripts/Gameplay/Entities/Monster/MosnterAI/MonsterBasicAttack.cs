using UnityEngine;

public class MonsterBasicAttack
{
    
    public void ExecuteBasicAttack(EntityRuntimeData runtimeData)
    {
        //Debug.Log($"{runtimeData.Transform.name} performs a basic attack.");
        Debug.Log($"performs a basic attack.");
    }
}